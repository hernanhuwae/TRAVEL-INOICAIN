import 'package:flutter/material.dart';
import '../share/allTheme.dart';

class transaksiInoicain extends StatelessWidget {
  const transaksiInoicain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [Text('Transaksi Saya')],
        ),
      ),
    );
  }
}
