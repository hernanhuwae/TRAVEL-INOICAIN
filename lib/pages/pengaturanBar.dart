import 'package:flutter/material.dart';
import '../share/allTheme.dart';

class pengaturanInoicain extends StatelessWidget {
  const pengaturanInoicain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [Text('PENGATURAN')],
        ),
      ),
    );
  }
}
