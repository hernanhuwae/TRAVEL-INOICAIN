import 'package:flutter/material.dart';
import '../share/allTheme.dart';

class dompetTravelInoicain extends StatelessWidget {
  const dompetTravelInoicain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [Text('Dompet Saya')],
        ),
      ),
    );
  }
}
